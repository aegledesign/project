'use client';
import { useMemo, useState } from 'react';
import type { Product } from '@/lib/types';
import { getUnitPrice, totalQty } from '@/lib/pricing';
import { useCart } from '@/lib/cartClient';
export function ProductConfigurator({product}:{product:Product}){ const [color,setColor]=useState(product.colors[0]); const [loc,setLoc]=useState(product.printLocations[0]); const [sizes,setSizes]=useState<Record<string,number>>({[product.sizes[0]]:12}); const qty=totalQty(sizes); const unit=useMemo(()=>getUnitPrice(product, qty),[product,qty]); const add=useCart(s=>s.add); return <div className="mt-8 space-y-6"><div><label className="label">Color</label><div className="mt-2 flex flex-wrap gap-2">{product.colors.map(c=><button key={c} onClick={()=>setColor(c)} className={`rounded-full border px-4 py-2 text-sm font-bold ${c===color?'bg-ink text-white':'bg-white'}`}>{c}</button>)}</div></div><div><label className="label">Print location</label><select className="input mt-2" value={loc} onChange={e=>setLoc(e.target.value)}>{product.printLocations.map(l=><option key={l}>{l}</option>)}</select></div><div><label className="label">Sizes / quantities</label><div className="mt-2 grid grid-cols-3 gap-2">{product.sizes.map(s=><div key={s}><div className="mb-1 text-xs font-bold">{s}</div><input className="input" type="number" min="0" value={sizes[s]??0} onChange={e=>setSizes({...sizes,[s]:Number(e.target.value)})}/></div>)}</div></div><div className="rounded-3xl bg-white p-5 shadow"><div className="flex justify-between"><span>Total qty</span><b>{qty}</b></div><div className="flex justify-between"><span>Unit price</span><b>${unit.toFixed(2)}</b></div><div className="mt-2 flex justify-between border-t pt-2 text-xl"><span>Total</span><b>${(qty*unit).toFixed(2)}</b></div><button className="btn-primary mt-4 w-full" onClick={()=>add({id:crypto.randomUUID(), productSlug:product.slug, productName:product.name, color, printLocation:loc, sizes, unitPrice:unit})}>Add configured item to cart</button></div></div> }
