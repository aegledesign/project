'use client';
import { useState } from 'react';
import { useCart } from '@/lib/cartClient';
export default function Checkout(){ const {items,total,clear}=useCart(); const [done,setDone]=useState(false); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault(); const fd=new FormData(e.currentTarget); await fetch('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({customerName:fd.get('name'),email:fd.get('email'),notes:fd.get('notes'),items,total:total()})}); clear(); setDone(true)} if(done) return <main className="mx-auto max-w-3xl px-6 py-16"><div className="card p-8"><h1 className="text-4xl font-black">Order request received</h1><p className="mt-3 text-slate-600">A proof and final quote can now be managed in the admin backend.</p></div></main>; return <main className="mx-auto max-w-4xl px-6 py-12"><h1 className="text-5xl font-black">Checkout / quote request</h1><p className="mt-3 text-slate-600">This local build saves a quote/order request. Production can switch this button to Stripe Checkout.</p><form onSubmit={submit} className="card mt-8 grid gap-4 p-6"><input className="input" name="name" placeholder="Customer name" required/><input className="input" name="email" type="email" placeholder="Email" required/><textarea className="input min-h-32" name="notes" placeholder="Deadline, decoration method, shipping address, special notes"/><div className="rounded-2xl bg-slate-50 p-4"><b>{items.length}</b> cart line(s) · Estimated total <b>${total().toFixed(2)}</b></div><button className="btn-primary">Submit order request</button></form></main> }
