export const metadata = {
  title: 'Design Studio | Aegle Custom',
  description: 'Customize a product using backend-managed mockups and printable areas.',
}

export default function DesignLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
