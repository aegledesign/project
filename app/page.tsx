import Link from 'next/link';
import { ArrowRight, Palette, Users, ClipboardCheck, Factory } from 'lucide-react';
import { getActiveProducts } from '@/lib/products';
import { getContent } from '@/lib/content';
import { ProductCard } from '@/components/ProductCard';
export default async function Home(){ const products=await getActiveProducts(); const content=await getContent(); return <main><section className="bg-gradient-to-br from-cream to-white"><div className="mx-auto grid max-w-7xl items-center gap-10 px-6 py-20 md:grid-cols-2"><div><div className="mb-5 inline-flex rounded-full bg-white px-4 py-2 text-sm font-bold text-ocean shadow">{content.announcement}</div><h1 className="text-5xl font-black leading-tight md:text-7xl">{content.heroTitle}</h1><p className="mt-6 text-xl text-slate-600">{content.heroSubtitle}</p><div className="mt-8 flex flex-wrap gap-3"><Link className="btn-primary" href="/catalog">Start an order <ArrowRight className="inline" size={16}/></Link><Link className="btn-secondary" href="/group-orders">Create group order</Link></div></div><div className="card p-6"><div className="rounded-3xl bg-slate-900 p-8 text-white"><h2 className="text-2xl font-black">Design Studio</h2><p className="mt-2 text-slate-300">Upload logos, add text, choose decoration method, collect sizes, and approve proofs.</p><div className="mt-8 grid gap-4 sm:grid-cols-2">{[[Palette,'Design online'],[Users,'Group ordering'],[ClipboardCheck,'Proof approval'],[Factory,'Production tracking']].map(([Icon,label]:any)=><div key={label} className="rounded-2xl bg-white/10 p-4"><Icon/><div className="mt-3 font-bold">{label}</div></div>)}</div></div></div></div></section><section className="mx-auto max-w-7xl px-6 py-16"><div className="flex items-end justify-between"><div><h2 className="text-3xl font-black">Popular products</h2><p className="mt-2 text-slate-600">Choose a product and launch the design studio.</p></div><Link href="/catalog" className="font-bold text-ocean">View all</Link></div><div className="mt-8 grid gap-6 md:grid-cols-4">{products.slice(0,4).map(p=><ProductCard key={p.id} product={p}/>)}</div></section><section className="mx-auto max-w-7xl px-6 py-16"><div className="card grid gap-8 p-8 md:grid-cols-3"><Feature title="1. Pick a product" text="Browse apparel, hats, bags, uniforms, and promo products."/><Feature title="2. Design or upload art" text="Use the design studio or request artwork cleanup from the team."/><Feature title="3. Approve proof & produce" text="Admin backend tracks quote, proof, payment, production, and shipping."/></div></section></main> }
function Feature({title,text}:{title:string;text:string}){return <div><h3 className="text-xl font-black">{title}</h3><p className="mt-2 text-slate-600">{text}</p></div>}
