import { getActiveProducts } from '@/lib/products';
import { ProductCard } from '@/components/ProductCard';
export default async function Catalog(){ const products=await getActiveProducts(); const cats=[...new Set(products.map(p=>p.category))]; return <main className="mx-auto max-w-7xl px-6 py-12"><h1 className="text-5xl font-black">Product catalog</h1><p className="mt-3 text-lg text-slate-600">Editable from the admin backend. Products support price breaks, colors, sizes, SKUs, tags, and print locations.</p><div className="mt-8 flex flex-wrap gap-2">{cats.map(c=><span key={c} className="rounded-full bg-white px-4 py-2 text-sm font-bold shadow">{c}</span>)}</div><div className="mt-10 grid gap-6 md:grid-cols-3 lg:grid-cols-4">{products.map(p=><ProductCard key={p.id} product={p}/>)}</div></main> }
