export default function GroupOrders(){ return <main className="mx-auto max-w-6xl px-6 py-12"><h1 className="text-5xl font-black">Group orders</h1><p className="mt-4 max-w-3xl text-lg text-slate-600">Create a team store link, let each participant choose sizes and quantities, collect payments, and close the order when ready for production.</p><div className="mt-8 grid gap-6 md:grid-cols-3">{['Create group link','Collect sizes and payments','Approve production'].map((t,i)=><div key={t} className="card p-6"><div className="text-4xl font-black text-ocean">0{i+1}</div><h2 className="mt-3 text-2xl font-black">{t}</h2><p className="mt-2 text-slate-600">Backend-ready workflow for team stores, school events, reunions, and corporate campaigns.</p></div>)}</div><form className="card mt-10 grid gap-4 p-6"><h2 className="text-2xl font-black">Create group order draft</h2><input className="input" placeholder="Group / event name"/><input className="input" placeholder="Close date"/><textarea className="input" placeholder="Instructions for participants"/><button type="button" className="btn-primary">Generate draft group order</button></form></main> }
